SELECT COUNTRY_NAME, REGION_NAME FROM COUNTRIES C INNER JOIN REGIONS R ON R.REGION_ID = C.REGION_ID;

SELECT FIRST_NAME, JOB_TITLE FROM EMPLOYEES e INNER JOIN JOBS J on e.JOB_ID = j.JOB_ID;

SELECT DEPARTMENT_NAME, CITY FROM DEPARTMENTS d INNER JOIN LOCATIONS l on d.LOCATION_ID = l.LOCATION_ID;

